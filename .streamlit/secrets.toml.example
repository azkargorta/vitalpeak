# Streamlit Cloud Secrets
#
# In Streamlit Cloud: open your app → Manage app → Settings → Secrets
# and paste these keys (replacing the API key).

OPENAI_API_KEY = "sk-PEGA_AQUI_TU_API_KEY"
OPENAI_MODEL = "gpt-4o-mini"
