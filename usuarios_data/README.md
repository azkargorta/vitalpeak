# Datos locales de usuario

Esta carpeta guarda un JSON por usuario (`<nombre>.json`) cuando la app corre en tu PC.

- **No se sube a GitHub** (está en `.gitignore`).
- En **Streamlit Cloud** el disco es temporal; los datos deben ir a **Supabase** (Fase 2 de `NOTA_PUBLICAR.md`).
